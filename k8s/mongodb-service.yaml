apiVersion: v1
kind: Service
metadata:
  name: mongodb-service
  namespace: shoppilot
  labels:
    app: shoppilot
    component: database
spec:
  type: ClusterIP
  ports:
    - port: 27017
      targetPort: 27017
  selector:
    app: shoppilot
    component: database
